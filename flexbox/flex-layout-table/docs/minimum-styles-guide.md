# Flexboxレイアウト実装ガイド：最低限必要なスタイル

このドキュメントでは、Header、Table（スティッキーヘッダー付き）、Preview、Footerのレイアウトを実現するために**最低限必要なスタイル**を解説します。

---

## 📋 目次

1. [全体構造の概要](#全体構造の概要)
2. [垂直レイアウト（App）](#1-垂直レイアウトapp)
3. [Header（固定ヘッダー）](#2-header固定ヘッダー)
4. [Main（中央エリア）](#3-main中央エリア)
5. [横方向50/50分割（Table + Preview）](#4-横方向5050分割table--preview)
6. [スティッキーテーブルヘッダー](#5-スティッキーテーブルヘッダー)
7. [Footer（固定フッター）](#6-footer固定フッター)
8. [よくある間違いと解決方法](#よくある間違いと解決方法)
9. [完全なコード例](#完全なコード例)

---

## 全体構造の概要

```
┌─────────────────────────────────────────┐
│  App Container                          │
│  (display: flex, flex-direction:column) │
│  ┌───────────────────────────────────┐  │
│  │ Header (固定)                     │  │
│  ├───────────────────────────────────┤  │
│  │ Main (flex: 1)                    │  │
│  │  ┌──────────────┬───────────────┐ │  │
│  │  │ Table        │ Preview       │ │  │
│  │  │ Container    │ Container     │ │  │
│  │  │ (flex: 1)    │ (flex: 1)     │ │  │
│  │  │              │               │ │  │
│  │  │ ┌──────────┐ │               │ │  │
│  │  │ │Sticky Hdr│ │               │ │  │
│  │  │ ├──────────┤ │               │ │  │
│  │  │ │  Row 1   │ │               │ │  │
│  │  │ │  Row 2   │ │ (scrollable)  │ │  │
│  │  │ │  Row 3   │ │               │ │  │
│  │  │ │  ...     │ │               │ │  │
│  │  │ └──────────┘ │               │ │  │
│  │  │ (scrollable) │               │ │  │
│  │  └──────────────┴───────────────┘ │  │
│  ├───────────────────────────────────┤  │
│  │ Footer (固定)                     │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 1. 垂直レイアウト（App）

### 必須スタイル

```scss
// App.module.scss
.app {
  display: flex;           // ① Flexboxを有効化
  flex-direction: column;  // ② 縦方向に配置（Header → Main → Footer）
  height: 100vh;          // ③ ビューポート全体の高さを使用
}
```

### 各プロパティの役割

| プロパティ | 値 | 理由 |
|-----------|-----|------|
| `display` | `flex` | 子要素（Header, Main, Footer）をFlexboxで制御 |
| `flex-direction` | `column` | 縦方向に並べる（デフォルトは`row`なので必須） |
| `height` | `100vh` | **最重要！** これがないとFlexboxが正しく動作しない |

**⚠️ 重要:** `height: 100vh` を省略すると、`.main` の `flex: 1` が効かず、レイアウトが崩れます。

---

## 2. Header（固定ヘッダー）

### 必須スタイル

```scss
// Header.module.scss
.header {
  flex-shrink: 0;  // ① 縮まないように固定（省略可）
  // その他の装飾は自由（背景色、パディングなど）
}
```

### ポイント

- `flex-shrink: 0` は**省略しても動作します**が、明示的に指定すると意図が明確
- Flexboxの子要素として、**コンテンツの高さ（content-based）で自動的に固定**されます
- `flex: 0 0 auto` と書くこともできますが、デフォルト値なので不要

---

## 3. Main（中央エリア）

### 必須スタイル

```scss
// App.module.scss
.main {
  display: flex;     // ① 横方向のFlexboxを有効化
  flex: 1;          // ② 残りのスペースを全て占有
  overflow: hidden; // ③ 自身はスクロールせず、子要素に委譲
}
```

### 各プロパティの役割

| プロパティ | 値 | 理由 |
|-----------|-----|------|
| `display` | `flex` | 子要素（Table, Preview）を横並びで制御 |
| `flex` | `1` | **最重要！** HeaderとFooterを除いた残りスペースを全て占有 |
| `overflow` | `hidden` | `.main`自体はスクロールせず、TableとPreviewがそれぞれスクロール |

**📌 重要:** `flex: 1` は `flex: 1 1 0%` の省略形で、以下を意味します：
- `flex-grow: 1` - 余白があれば成長
- `flex-shrink: 1` - 縮小が必要なら縮小
- `flex-basis: 0%` - 初期サイズは0から開始

---

## 4. 横方向50/50分割（Table + Preview）

### 必須スタイル

#### EmailTable.module.scss
```scss
.tableContainer {
  flex: 1;        // ① 50%の幅を確保
  overflow: auto; // ② 縦方向にスクロール可能
}
```

#### EmailPreview.module.scss
```scss
.previewContainer {
  flex: 1;        // ① 50%の幅を確保
  overflow: auto; // ② 縦方向にスクロール可能
}
```

### 🔥 最重要ポイント：`flex: 1` vs `flex: 1 1 auto`

```scss
// ❌ 間違い - コンテンツ幅に依存してしまう
.tableContainer {
  flex: 1 1 auto;  // flex-basis: auto → Previewが広くなる（1:4の比率）
}
.previewContainer {
  flex: 1 1 auto;  // flex-basis: auto → コンテンツ幅で決まる
}

// ✅ 正解 - 均等に分割される
.tableContainer {
  flex: 1;  // flex-basis: 0 → 50:50の比率
}
.previewContainer {
  flex: 1;  // flex-basis: 0 → 50:50の比率
}
```

### なぜ `flex: 1` が必要か？

| 値 | `flex-basis` | 結果 |
|----|-------------|------|
| `flex: 1 1 auto` | `auto`（コンテンツ幅） | Previewの方がコンテンツが多いため、1:4の比率になる |
| `flex: 1` | `0`（ゼロスタート） | コンテンツ幅を無視して、50:50で均等分割 |

**📖 解説:**
- `flex-basis: auto` は「コンテンツの自然な幅を基準にする」という意味
- Previewには長い文章が入るため、Tableより広くなる
- `flex-basis: 0` は「ゼロから開始して、利用可能なスペースを均等に分配」という意味

---

## 5. スティッキーテーブルヘッダー

### 必須スタイル

```scss
// EmailTableHeader.module.scss
.tableHeader {
  position: sticky;           // ① スクロール時に固定
  top: 0;                     // ② 上端に固定
  z-index: 10;                // ③ 他の要素より前面に表示
  background-color: #ffffff;  // ④ 背景色（必須！）
}
```

### 各プロパティの役割

| プロパティ | 値 | 理由 |
|-----------|-----|------|
| `position` | `sticky` | **最重要！** スクロール時に位置を固定 |
| `top` | `0` | **必須！** どの位置に固定するかを指定（上端から0px） |
| `z-index` | `10` | 行が下に流れても、ヘッダーが前面に表示される |
| `background-color` | `#ffffff` | **必須！** ないとスクロール時に下の行が透けて見える |

### よくある間違い

```scss
// ❌ これだけでは動作しない
.tableHeader {
  position: sticky;  // topがないので固定されない
}

// ❌ 背景色がないと透ける
.tableHeader {
  position: sticky;
  top: 0;
  // background-colorがないと、下の行が透けて見える
}

// ✅ 正しい実装
.tableHeader {
  position: sticky;
  top: 0;
  z-index: 10;
  background-color: #ffffff;  // 必須！
}
```

### 動作イメージ

```
スクロール前:
┌──────────────┐
│ Table Header │ ← ヘッダー
├──────────────┤
│ Row 1        │
│ Row 2        │
│ Row 3        │
└──────────────┘

スクロール後:
┌──────────────┐
│ Table Header │ ← 固定されたまま（position: sticky）
├──────────────┤
│ Row 5        │
│ Row 6        │
│ Row 7        │
└──────────────┘
```

---

## 6. Footer（固定フッター）

### 必須スタイル

```scss
// Footer.module.scss
.footer {
  flex-shrink: 0;  // 縮まないように固定（省略可）
  // その他の装飾は自由
}
```

### ポイント

- Headerと同様、**コンテンツの高さで自動的に固定**されます
- 特別なスタイルは不要

---

## よくある間違いと解決方法

### ❌ 問題1: Mainが伸びない

```scss
// 間違い
.app {
  display: flex;
  flex-direction: column;
  // height: 100vh; がない！
}
```

**解決方法:**
```scss
.app {
  display: flex;
  flex-direction: column;
  height: 100vh;  // これを追加！
}
```

---

### ❌ 問題2: TableとPreviewが50:50にならない

```scss
// 間違い
.tableContainer {
  flex: 1 1 auto;  // flex-basis: auto なので不均等
}
.previewContainer {
  flex: 1 1 auto;
}
```

**解決方法:**
```scss
.tableContainer {
  flex: 1;  // flex-basis: 0 で均等分割
}
.previewContainer {
  flex: 1;
}
```

---

### ❌ 問題3: テーブルヘッダーが固定されない

```scss
// 間違い
.tableHeader {
  position: sticky;  // topがない！
}
```

**解決方法:**
```scss
.tableHeader {
  position: sticky;
  top: 0;  // これを追加！
  background-color: #ffffff;  // これも必須！
}
```

---

### ❌ 問題4: スクロール時にヘッダーの下が透ける

```scss
// 間違い
.tableHeader {
  position: sticky;
  top: 0;
  // background-colorがない！
}
```

**解決方法:**
```scss
.tableHeader {
  position: sticky;
  top: 0;
  background-color: #ffffff;  // 背景色を追加！
  z-index: 10;  // 確実に前面に表示
}
```

---

## 完全なコード例

### HTML構造（React/TSX）

```tsx
// App.tsx
import styles from './App.module.scss'

export default function App() {
  return (
    <div className={styles.app}>
      <Header />

      <main className={styles.main}>
        <EmailTable />
        <EmailPreview />
      </main>

      <Footer />
    </div>
  )
}
```

### 最低限必要なSCSS

```scss
// App.module.scss
.app {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.main {
  display: flex;
  flex: 1;
  overflow: hidden;
}
```

```scss
// Header.module.scss
.header {
  flex-shrink: 0;
  // 装飾スタイル（背景色、パディングなど）
}
```

```scss
// EmailTable.module.scss
.tableContainer {
  flex: 1;
  overflow: auto;
}

.table {
  width: 100%;
  border-collapse: collapse;
}
```

```scss
// EmailTableHeader.module.scss
.tableHeader {
  position: sticky;
  top: 0;
  z-index: 10;
  background-color: #ffffff;
  border-bottom: 2px solid #d1d5db;
}
```

```scss
// EmailPreview.module.scss
.previewContainer {
  flex: 1;
  overflow: auto;
  border-left: 1px solid #d1d5db;
  background-color: #ffffff;
  padding: 1.5rem;
}
```

```scss
// Footer.module.scss
.footer {
  flex-shrink: 0;
  // 装飾スタイル
}
```

---

## まとめ：最低限必要なスタイル一覧

| コンポーネント | 必須スタイル | 役割 |
|------------|------------|------|
| **App** | `display: flex`<br>`flex-direction: column`<br>`height: 100vh` | 垂直レイアウトの基盤 |
| **Main** | `display: flex`<br>`flex: 1`<br>`overflow: hidden` | 横レイアウト + 残りスペース占有 |
| **Header** | （特になし） | 自然な高さで固定 |
| **Footer** | （特になし） | 自然な高さで固定 |
| **TableContainer** | `flex: 1`<br>`overflow: auto` | 50%幅 + スクロール可能 |
| **TableHeader** | `position: sticky`<br>`top: 0`<br>`z-index: 10`<br>`background-color` | スクロール時に固定 |
| **PreviewContainer** | `flex: 1`<br>`overflow: auto` | 50%幅 + スクロール可能 |

---

## 追加リソース

- [MDN: Flexbox](https://developer.mozilla.org/ja/docs/Web/CSS/CSS_Flexible_Box_Layout)
- [MDN: position: sticky](https://developer.mozilla.org/ja/docs/Web/CSS/position#sticky)
- [プロジェクトREADME](../README.md)

---

**作成日**: 2026-02-13
**プロジェクト**: Email Viewer - Flexbox Demo
