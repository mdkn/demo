// Footer component with SCSS Modules
import styles from './Footer.module.scss'

const Footer = () => {
  return <footer className={styles.footer}>Flexbox Layout Demo</footer>
}

export default Footer